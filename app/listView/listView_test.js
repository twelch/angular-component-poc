'use strict';

describe('myApp.listView module', function() {

  beforeEach(module('myApp.listView'));

  describe('listView controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var listViewCtrl = $controller('listViewCtrl');
      expect(listViewCtrl).toBeDefined();
    }));

  });
});